def add_numbers(a = 7,  b = 8):
    """
    add the numbers together

    Arguments:
        a: one number
        b: the other number

    Returns:
        float: the addition of the two numbers
    """

    sum = float(a) + float(b)
    print('Sum:', str(sum))


